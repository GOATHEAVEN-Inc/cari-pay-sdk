[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "caripay-sdk"
version = "1.5.0"
description = "CARI PAY Gateway API client"
requires-python = ">=3.9"
license = { text = "Proprietary" }

[tool.setuptools]
py-modules = ["caripay"]
